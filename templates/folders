<ul class="directory">
{{range $dindex, $directory := .}}
	<li>{{.Name}}
	<ul class="folder">
	{{range $findex, $folder := .Folders}}
		<li><a href="folder?directory={{$dindex}}&folder={{$findex}}">{{.Name}}</a></li>
	{{end}}
	</ul></li>
{{end}}
</ul>
